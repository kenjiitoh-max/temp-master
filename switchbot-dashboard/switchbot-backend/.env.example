# SwitchBot API Credentials
# Get these from the SwitchBot app:
# 1. Go to Profile > Preferences > About
# 2. Tap App Version 10 times to enable Developer Options
# 3. Go to Developer Options > Get Token

SWITCHBOT_TOKEN=your_token_here
SWITCHBOT_SECRET=your_secret_here

# Shared secret required to access GET /api/backup.
# Clients must send it as "Authorization: Bearer <token>".
# If left empty/unset, the /api/backup endpoint is DISABLED (returns 503)
# rather than being left open to anyone.
BACKUP_API_TOKEN=your_backup_token_here
