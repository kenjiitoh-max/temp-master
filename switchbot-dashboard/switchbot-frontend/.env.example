# Base URL of the backend API.
# Leave empty to use the same origin (recommended when the built frontend is
# served by the FastAPI backend). When opened directly as a file:// URL, the
# app falls back to the production backend automatically.
VITE_API_URL=
