# Optional: override the API base URL used by the frontend.
# Leave empty to call the backend on the same origin (default when the SPA is
# served by FastAPI, and during local dev where Vite proxies /api to :8000).
# When opened directly from the filesystem (file://), the app falls back to
# the production backend automatically.
VITE_API_URL=
