# Base URL of the FastAPI backend.
# Leave empty when the frontend is served from the backend (same origin).
# For local `npm run dev` against a local backend, use http://localhost:8000
VITE_API_URL=http://localhost:8000
