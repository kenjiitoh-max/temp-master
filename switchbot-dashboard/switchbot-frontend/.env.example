# Optional override for the backend API base URL.
# Leave unset for same-origin serving (production / behind the FastAPI backend).
# When developing with `npm run dev`, requests to /api are proxied to
# http://localhost:8000 (see vite.config.ts), so this can stay empty.
# VITE_API_URL=https://temp-master.fly.dev
